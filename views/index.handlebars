<h1>Burgers!</h1>


<ul>
  {{#each burgers}}
    <li>
      <p>
        {{this.burger_name}}
        <button data-movieid="{{this.id}}" class=".change-devour">DEVOUR IT!</button>
      </p>
    </li>
  {{/each}}
</ul>



{{!-- <ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burger-block devoured=true}}
    {{/unless}}
  {{/each}}
</ul>


<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/burger-block devoured=false}}
    {{/if}}
  {{/each}}
</ul> --}}

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Enter a burger you want to eat:</label>
    <input id="ca" type="text" name="name">
  </div>

  <button type="submit">SUBMIT</button>
</form>



